version: '3'

services:
  linux:
    image: jenkins-agent-linux
    restart: always
    environment:
      --JENKINS_URL=
      --JENKINS_SECRET=
      --JENKINS_AGENT_NAME=
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
